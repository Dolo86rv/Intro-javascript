const activo = true

let mensaje = (activo) ? 'Activo' : 'Inactivo'

const message = activo && 'Activo'

/*if(activo)
{
    mensaje = 'Activo'
} else {
    mensaje = 'Inanctivo'
}
*/

console.log(message)
console.log(mensaje)



